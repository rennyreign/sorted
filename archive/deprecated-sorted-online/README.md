# Deprecated Sorted Online Archive

**Archived:** 2026-07-22
**Status:** Local reference only. Do not route new traffic here.

This folder preserves the previous Sorted public website direction: the small-business website offer centred on "get picked, get enquiries, get bought."

The active brand is now:

- **Sorted**: business modernisation company.
- **Sorted Sites**: website offer, active at `/sites` and intended for `sortmydigital.site`.
- **Sorted Ops**: operations and process-improvement offer, active at `/ops` and intended for `ops.sortmydigital.site`.

Archived files:

- `app/about/page.tsx`
- `app/examples/page.tsx`
- legacy marketing components used by the previous public site.

The active `/about` and `/examples` routes now point to the new Sorted Sites pages so existing links do not land on deprecated positioning.

Do not archive or modify operator, prospect finder, CRM, client delivery, proposal, or SortedUpdates routes as part of this brand migration.
