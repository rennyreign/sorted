RewriteEngine On

# Rewrite /review/* to /review/index.html (serves the static SPA shell)
# The client-side code reads the slug from window.location.pathname
RewriteRule ^review/(.+)/?$ /review/index.html [L,NC]
