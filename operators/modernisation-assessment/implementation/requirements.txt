requests>=2.31.0
python-dotenv>=1.0.0
beautifulsoup4>=4.12.0
# Optional: only needed if you want Playwright screenshots instead of HTML-only evidence.
# playwright is not installed by default because of large browser binaries.
# To enable: pip install playwright && playwright install chromium
